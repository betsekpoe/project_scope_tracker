# **Project Scope tracker Overview**



Open a project,

Give the original project scope with time and date,

Adds every new request with a date
Generates visual timeline of scope changes

Each request contains the time, what changed, what was before and after

PACKAGES USED:
- tailwindcss
- flaticon-uicons (regular-rounded)

